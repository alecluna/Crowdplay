import React from "react";
import Header from "../Components/Header";
import Hero from "../Components/Hero";

const Home = () => (
  <div>
    <Header />
    <Hero />
  </div>
);

export default Home;
